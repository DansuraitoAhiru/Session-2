#include <stdio.h>
#include <math.h>
int main(){
    int a = 50;
    int b = 26;
    int P = (a+b) * 2;
    printf("p = %d \n", P);
    int S = a * b;
    printf("S = %d \n", S);
    return 0;
}
